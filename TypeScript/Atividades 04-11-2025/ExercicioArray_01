//Escreva um programa para criar uma Collection Array de Objetos do tipo string. O programa deverá 
//solir ao usuário, que ele digite via teclado 5 cores e deverá adicioná-las individualmente no 
//Array. Em seguida, faça o que se pede: Mostre na tela todas as cores adicionadas. 
//Mostre na tela todas as cores adicionadas, ordenadas em ordem crescente.

import readlinesync = require("readline-sync");

let cores: Array<string> = new Array<string>(5);

console.log("Digite 5 cores: ");
for (let i = 0; i < cores.length; i++) {
    let cor = readlinesync.question(`Cor ${i + 1}: `);
    cores[i] = cor;
}
console.log("\nCores adicionadas: ");
for (let cor of cores) {
    console.log(cor);
}
cores.sort();
console.log(`\nCores ordenadas por ordem lexicografica:`);
for (let cor of cores) {
    console.log(cor);
}



